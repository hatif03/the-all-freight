[project]
name = "logistics"
version = "0.1.0"
description = "Logistics Agent (LangGraph)"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
package-dir = {"logistics" = "."}
packages = ["logistics"]
