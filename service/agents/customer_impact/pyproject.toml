[project]
name = "customer-impact"
version = "0.1.0"
description = "Customer-Impact Agent"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
package-dir = {"customer_impact" = "."}
packages = ["customer_impact"]
