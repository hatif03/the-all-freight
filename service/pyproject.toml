[project]
name = "ops-room"
version = "0.1.0"
description = "The All Freight — ops room agents monorepo"
requires-python = ">=3.12"
dependencies = [
    # Every agent imports backend/database.py + backend/models.py (via
    # PYTHONPATH, see Makefile/Procfile.dev) for room-bus/DB access, so this
    # shared workspace env needs the backend's DB stack too, not just the
    # per-framework agent libraries below.
    "sqlalchemy>=2.0.50",
    "asyncpg>=0.29.0",
    "pydantic-settings>=2.14.1",
    "redis>=5.0.0",
    "python-dotenv>=1.0.0",
    "openai>=1.50.0",
    "langgraph>=0.2.0",
    "langchain-openai>=0.2.0",
    "pydantic-ai>=0.0.20",
    "crewai>=1.14.7",
    "httpx>=0.27.0",
    "google-auth>=2.35.0",
]

[tool.uv]
managed = true

[tool.setuptools]
packages = []

[tool.uv.workspace]
members = ["agents/*"]
# Every agent runs its main.py as a top-level script importing sibling modules
# (protocol, registry, room_bus) via PYTHONPATH, so Python caches their bytecode
# in agents/__pycache__/ — which matches the members glob above and breaks every
# subsequent `uv run` in this workspace ("missing a pyproject.toml") until it's
# removed. Exclude it explicitly rather than relying on remembering to clean it up.
exclude = ["agents/__pycache__"]
